preference_id = '<%= @preference_id %>'
const mp = new MercadoPago('<%= ENV["MP_PUBLIC_KEY"] %>', {locale: 'es-CL'});
const checkout = mp.checkout({
  preference: {
      id: preference_id
  },
  autoOpen: true,
});
$("#buy_modal").modal('hide');
setTimeout(function(){
  $('.mp-mercadopago-checkout-wrapper').remove();
}, 10000);
