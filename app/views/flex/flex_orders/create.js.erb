var preference_id = '<%= @preference_id %>'
var mp = new MercadoPago('<%= ENV["MP_PUBLIC_KEY"] %>', {locale: 'es-CL'});
var checkout = mp.checkout({
  preference: {
      id: preference_id
  },
  autoOpen: true,
});
$("#buy_modal").modal('hide');
