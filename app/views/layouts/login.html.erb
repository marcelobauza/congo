<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render :partial => 'layouts/head' %>
  </head>
  <body class="<%= params['module'] == 'flex' ? 'login' : 'login_flex' %>" >

    <%= yield %>

  </body>
</html>
