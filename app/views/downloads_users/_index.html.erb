<div class="modal fade" id="modal-downloads-users" tabindex="-1" role="dialog">
  <div class="modal-dialog modal-xl" role="document">
    <div class="modal-content text-light">

      <div class="modal-header">
        <h5 class="modal-title">Descargas</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body">

        <div class="form-group row">
          <div class="col-md-5 text-right">
            Descargas Compraventas
          </div>
          <div class="col-md-6">
              <span class="col-md-4 text-left"><%= accumulated_download_by_company('transactions') %></span>
              <span class="col-md-4"><%= current_user.company.transactions_downloads %></span>
              <span class="col-md-4 text-right text-danger"><%= surplus_downloads('transactions') %></span>
          </div>
        </div>
        <div class="form-group row">
          <div class="col-md-5 text-right">
            Descargas Expedientes
          </div>
            <div class="col-md-7">
              <span class="col-md-4 text-left"><%= accumulated_download_by_company('future_projects') %></span>
              <span class="col-md-4"><%= current_user.company.future_projects_downloads %></span>
              <span class="col-md-4 text-right text-danger"><%= surplus_downloads('future_projects') %></span>
          </div>
        </div>
        <div class="form-group row">
          <div class="col-md-5 text-right">
            Descargas Proyectos
          </div>
            <div class="col-md-6">
              <span class="col-md-4 text-left"><%= accumulated_download_by_company('projects') %></span>
              <span class="col-md-4"><%= current_user.company.projects_downloads %></span>
              <span class="col-md-4 text-right text-danger"><%= surplus_downloads('projects') %></span>
          </div>
        </div>

        <%= render :partial => "filters" %>
        <table class="table table-striped table-hover table-bordered">
          <thead>
            <tr>
              <th>Nombre</th>
              <th>CBR</th>
              <th>PRV</th>
              <th>EM</th>
              <th>Creación</th>
              <th class="text-center">Acciones</th>
            </tr>
          </thead>

          <tbody>
            <% @downloads_users.each do |du| %>
              <tr>
                <td><%= du.title %></td>
                <td><%= du.transactions %></td>
                <td><%= du.projects %></td>
                <td><%= du.future_projects %></td>
                <td><%= l(du.created_at, format: "%d %B, %Y") %></td>
                <td class="text-center"><%= link_to downloads_users_reports_by_layer_path(id: du.id) do %><i class="fas fa-download" title="Descargar"></i><% end %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
          <div class="row">
            <div class="col-6">
              <%= js_will_paginate @recipes %>
            </div>
            <div class="col-6 text-right">
              <%= link_to "Exportar descargas", users_export_csv_downloads_by_company_path %>
            </div>
          </div>
        </div>

    </div>
  </div>
</div>
