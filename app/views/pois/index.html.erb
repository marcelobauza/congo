<p id="notice"><%= notice %></p>

<h1>Pois</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Poi subcategory</th>
      <th>The geom</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pois.each do |poi| %>
      <tr>
        <td><%= poi.name %></td>
        <td><%= poi.poi_subcategory %></td>
        <td><%= poi.the_geom %></td>
        <td><%= link_to 'Show', poi %></td>
        <td><%= link_to 'Edit', edit_poi_path(poi) %></td>
        <td><%= link_to 'Destroy', poi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Poi', new_poi_path %>
