var errors = '<%= raw @errorMessage %>';

if (errors == '[]') {
  $("#modal-account").remove();
  $('.modal-backdrop').remove();
} else {
  $("#error_item").remove()
  $("#users_error").append(
    $('<p>', {
      'id': 'error_item',
      'text': errors
    })
  )
}
