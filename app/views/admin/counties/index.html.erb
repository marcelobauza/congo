<p id="notice"><%= notice %></p>

<h1>Counties</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Code</th>
      <th>Code sii</th>
      <th>Name last project future</th>
      <th>Enabled</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @counties.each do |county| %>
      <tr>
        <td><%= county.name %></td>
        <td><%= county.code %></td>
        <td><%= county.code_sii %></td>
        <td><%= county.number_last_project_future %></td>
        <td><%= county.enabled %></td>
        <td><%= link_to 'Edit', edit_admin_county_path(county) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
