<%= simple_form_for([:admin, @period]) do |f| %>

  <div class="form-inputs">
    <%= f.input :bimester %>
    <%= f.input :year %>
    <%= f.check_box :active %>
  </div>

  <div class="form-actions">
    <%= f.submit :submit %>
  </div>
<% end %>
